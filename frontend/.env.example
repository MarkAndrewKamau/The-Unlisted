# Copy to frontend/.env.local. Points the UI at the backend API (server.py).
# Local dev:
VITE_API_BASE=http://localhost:8000
# Production (e.g. Render): VITE_API_BASE=https://your-backend.onrender.com
