# Copy to onchain/.env (and the repo root for the Python relayer) and fill in.
# NEVER commit a real private key.

# Avalanche Fuji testnet
AVALANCHE_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Deployer / registry owner key (testnet only). Faucet: https://faucet.avax.network/
PRIVATE_KEY=

# Set after deploying (forge script prints it)
REGISTRY_ADDRESS=

# Optional: Snowtrace API key for contract verification
SNOWTRACE_API_KEY=
