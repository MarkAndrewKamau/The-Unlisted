# Core pipeline has zero hard dependencies (stdlib only) so it runs anywhere.
# These enable the live connectors and LLM profile synthesis.
requests>=2.31
beautifulsoup4>=4.12
anthropic>=0.40   # optional: only used by profile.py when ANTHROPIC_API_KEY is set

# HTTP API (server.py) for deploying the pipeline as a web service.
fastapi>=0.115
uvicorn>=0.32
python-dotenv>=1.0
pydantic[email]>=2.0

# Postgres persistence (discovery/store.py).
psycopg[binary]>=3.2

# Auth (discovery/auth.py) — bcrypt password hashing + JWT sessions.
bcrypt>=4.2
PyJWT>=2.9

# Avalanche on-chain provenance (discovery/onchain.py). Optional: only needed to
# SEND the publish transaction — hashing/Merkle are dependency-free (keccak.py),
# so dry-run root computation works without this.
web3>=6.20
